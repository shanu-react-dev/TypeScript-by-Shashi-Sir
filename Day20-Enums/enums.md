## ENum?

_ENUMS stands for Enumerations. It allows you to define a set of named constants._

_A enum is a special class in typescript that allows a developer to define set of named constants._

_Some data will never change like HTTP status code such as 200, 201, 500, 404..._
